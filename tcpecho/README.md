# tcpecho

Program that echoes data over TCP. Can be run in sender mode, listener mode or
both at the same time. Sender sends data, reads what it gets back and compares
if the same. Listener reads data and echoes it back. See `-h`.
